{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.new",
    "watchPatterns": [
      "apps/backend/**",
      "libs/**",
      "package.json",
      "pnpm-lock.yaml",
      "Dockerfile.new",
      "railway.json"
    ]
  },
  "deploy": {
    "startCommand": "node dist/apps/backend/src/main.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "environments": {
    "production": {
      "variables": {
        "NODE_ENV": "production",
        "PORT": "3000",
        "SUPABASE_URL": "https://placeholder.supabase.co",
        "SUPABASE_KEY": "placeholder-key-replace-with-real-values"
      }
    }
  }
}
