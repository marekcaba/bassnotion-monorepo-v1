import React from 'react';

// HorizontalLines component is not needed in the new approach
// Grid lines are integrated directly into the FretboardGrid component
export const HorizontalLines: React.FC = () => {
  return null;
};
