/** Difficulties that should be locked (require completing all unlocked exercises first) */
export const LOCKED_DIFFICULTIES = ['advanced', 'hard', 'expert'];

/** Number of practice completions required per exercise to mark it as done */
export const REQUIRED_COMPLETIONS = 4;
