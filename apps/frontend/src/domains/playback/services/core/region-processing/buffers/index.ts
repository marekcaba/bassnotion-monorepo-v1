/**
 * Buffer management modules
 *
 * Phase 3.2: BufferManager.ts deleted (was RegionProcessor-specific)
 * This directory now only contains index.ts as a placeholder
 * Buffer management is handled directly by PlaybackEngine
 */

// No exports - BufferManager was deleted in Phase 3.2
