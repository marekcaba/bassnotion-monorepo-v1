# Test environment configuration
NODE_ENV=test

# Supabase configuration (matching DatabaseService expectations)
SUPABASE_URL=http://localhost:54321
SUPABASE_KEY=test-service-role-key
SUPABASE_ANON_KEY=test-anon-key
SUPABASE_SERVICE_ROLE_KEY=test-service-role-key

# Database configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# API configuration
PORT=3000
API_PREFIX=api

# JWT configuration (required for tests)
JWT_SECRET=test-jwt-secret-for-backend-tests
JWT_EXPIRY=1h

# Rate limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# Test user credentials
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=ValidPassword123! 